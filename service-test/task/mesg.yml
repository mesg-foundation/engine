name: Task
description: "A service that only has tasks definition"
tasks:
  call:
    inputs:
      url:
        type: String
      data:
        type: Object
      headers:
        type: Object
    outputs:
      result:
        data:
          foo:
            type: Object