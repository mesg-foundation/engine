name: test-service
sid: test-service
tasks:
  ping:
    inputs:
      msg:
        type: String
    outputs:
      pong:
        type: String
  add:
    inputs:
      n:
        type: Number
      m:
        type: Number
    outputs:
      res:
        type: Number
  error:
events:
  ping_ok:
    data:
      msg:
        type: String
  add_ok:
    data:
      msg:
        type: String
  error_ok:
    data:
      msg:
        type: String
