version: "2"
services:
  listener:
    build: ./
    env_file: .env
    ports: 
      - 80:3000
    depends_on: 
      - redis
  redis:
    image: redis
    command: redis-server --appendonly yes
    volumes: 
      - ./data:/data