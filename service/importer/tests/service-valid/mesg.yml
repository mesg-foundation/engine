name: "Test service"
events:
  eventX:
    name: "EventX"
    data:
      foo:
        type: String
      bar:
        type: String
tasks:
  taskX:
    name: "TaskX"
    inputs:
      argA:
        type: String
      argB:
        type: String
    outputs:
      resX:
        type: String
      resY:
        type: String
dependencies:
  app:
    image: my-docker-image
    ports:
      - 80:80