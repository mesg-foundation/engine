name: # empty name
sid: foo#$bar # not valid
repository: ::// # invalid uri

events:
  foo:
    data: # no data
  bar:
    data:
      p1:
        type: # no type

tasks:
  foo:
    inputs: # no inputs
    outputs: # no outputs

configuration:
  volumes:
    - v1
    - v1 # not unique
  volumesfrom:
    - dummy # not existing
    - service # cyclic
  ports: # invalid ports
    - 0
    - 65536
    - 1:a
    - a:1
  env: # invalid env
    - foo

dependencies:
  service: # forbidden key
  dummy0:
    image: # empty image
    volumes:
      - v1
      - v1 # not unique
    volumesfrom:
      - none # not existing
    ports: # invalid ports
      - 0
      - 65536
      - 1:a
      - a:1
    env: 
      - _foo # invalid env name
  dummy1:
    image: image
    volumesfrom:
      - dummy1 # cyclic import
